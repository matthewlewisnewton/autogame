1. Sunken-canyon full playthrough still fails at telepipe-new-sortie: after magma-windup-ready card exercises, canyon-descent-telepipe-ready does not leave a charged usable weapon/spell for depleteRunResources (telepipe-only or telepipe + depleted creature observed).
   Files: game/server/debugScenarios.js (`setupQuestTelepipeReady`, `setupQuestTier2Deploy`, `ensurePlayerCombatHand`, `setupCanyonDescentTelepipeReadyExtras`), harness/validate/lib/telepipe.mjs (`runQuestTelepipeNewSortieStep` fromPlaying path)
   Fix: When telepipe-ready scenarios run via afterDeploy, force a fresh combat hand before applying extras — e.g. add a `forceRedeal` option to `ensurePlayerCombatHand` / `setupQuestTelepipeReady` that clears slots and calls `createDrawDeckFromSelectedDeck` + `initPlayerHand`, then set hand[0]=telepipe and hand[1]=magma_greatsword (full charges). Verify with `node ../harness/validate/playthrough.mjs --preset sunken-canyon --steps full`.
