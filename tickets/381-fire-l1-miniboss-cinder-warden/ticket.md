# 381-fire-l1-miniboss-cinder-warden

## Difficulty: medium

## Goal

Add a MINIBOSS (in-level stage-boss encounter) to the FIRE level-1 (Ember Descent, ember_descent/fire-cavern), which currently has none. Add the Cinder Warden as the stage-boss encounter via the encounter framework (258). Display metadata + lock-on panel + defeat objective. SCOPE: game/server + game/client + test.

## Verification

Run the harness checks (vitest server+client) and verify the acceptance criteria above.
