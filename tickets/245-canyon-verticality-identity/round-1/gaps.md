1. The top-level visual acceptance capture never enters the sunken-canyon layout: `sunken-canyon-stage` returned "Debug scenarios are disabled", `metrics.json` still reports `layout.profile: "crowded"` after the transition attempt, and `06-after-sunken-canyon.png` still shows the default training layout.
   Files: game/client/scripts/test-world-stage-transition.mjs, game/server/debugScenarios.js, game/server/index.js
   Fix: Make the visual QA path reach `layout.profile === "sunken-canyon"` in this environment, preferably by selecting/deploying the normal `canyon_descent` quest or by ensuring the dev-only `sunken-canyon-stage` shortcut is enabled only for scenario capture, then recapture screenshots/probes showing the plateau/canyon materials, cliff lips, monolith, lower camera, and hazard band.
