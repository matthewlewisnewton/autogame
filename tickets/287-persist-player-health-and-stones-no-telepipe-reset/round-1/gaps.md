1. Captured run is not green: `metrics.json` has `"ok": false` and `console.log` reports `Telepipe run-preservation assertion failed: suspended objective was not captured before assertRunPreserved`. The fallback capture recipe still depends on the `suspendedRunSummary` checkpoint state this ticket deliberately removed (recipe stashes `harnessState.suspendedRunSummary.objective`, which is now undefined, so `assertRunPreserved` throws).
   Files: harness/screenshot.mjs (stashObjective at ~line 358 / read at ~line 980; assertRunPreserved at ~lines 987-1070)
   Fix: update the telepipe capture recipe to the new in-memory pause contract — stash the objective from `harnessState.objective` (already present at the PRE-SUSPEND probe) instead of the removed `suspendedRunSummary`, and have `assertRunPreserved` verify run.status round-trips suspended→playing with the same run.id and preserved enemy set. Do NOT re-add `suspendedRunSummary` to game code (server.test.js:4881 enforces its absence). Re-run capture until `"ok": true`.
