Round 2 has a single open gap in `review-feedback.md`: the top-level harness capture dies before `#lobby` is visible because the backend stops accepting connections on the allocated port. Sub-tickets 01–04 all have `.passed` markers and were left unchanged.

## Created sub-ticket

- `/home/matt/workspace/.autogame-worktrees/232-hub-shared-presence/tickets/232-hub-shared-presence/subtickets/05-harness-capture-lobby-reachability/ticket.md`

**05-harness-capture-lobby-reachability** targets the server stability issue during capture (`ECONNREFUSED` / 502s on `/socket.io`) and client lobby-join sequencing in `game/server/index.js` and `game/client/main.js`, so the two-client capture reaches the squad lobby and produces `metrics.json` with `"ok": true`. Verification mode is `code` (metrics, logs, and lobby visibility are all checkable from artifacts).
