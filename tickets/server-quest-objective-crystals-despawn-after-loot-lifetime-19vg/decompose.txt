Warning: running headless with --yolo / approval-mode=yolo and no sandbox. All tool calls (shell, write, edit) auto-execute at this process's privilege level. Enable a sandbox via --sandbox / QWEN_SANDBOX, or set QWEN_CODE_SUPPRESS_YOLO_WARNING=1 to silence this notice.
Done. Created 2 sub-tickets:

```
tickets/server-quest-objective-crystals-despawn-after-loot-lifetime-19vg/subtickets/
├── 01-mark-crystals-quest-critical-and-skip-lifetime-filter/
│   └── ticket.md
└── 02-regression-test-crystals-survive-loot-lifetime/
    └── ticket.md
```

**01** — Add `questCritical: true` to crystals in `spawnCrystals()` and skip such items in the `LOOT_LIFETIME_MS` filter in `index.js`.

**02** — Regression test that advances the clock past `LOOT_LIFETIME_MS` and asserts quest-critical loot survives while ordinary loot expires.
