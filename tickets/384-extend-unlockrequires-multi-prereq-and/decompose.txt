Remediation round 3 has one open gap from `review-feedback.md`: tier-2 prerequisites cannot be satisfied through normal gameplay because completion is only inferred from a higher-tier unlock, and tier-2 victories are never persisted.

Sub-tickets 01–06 all have `.passed` markers and were left untouched. One new sub-ticket was added:

**Created:**
- `/home/matt/workspace/.autogame-worktrees/384-extend-unlockrequires-multi-prereq-and/tickets/384-extend-unlockrequires-multi-prereq-and/subtickets/07-tier-2-completion-persistence/ticket.md`

**07 — Persist tier-2 quest completion for multi-prereq AND gating** closes the gap by:
1. Adding a persisted `completedQuestTiers` map and `completeQuestTier()` helper in `users.js`
2. Updating `hasCompletedQuestTier()` to check explicit completion (with tier-1 inference preserved)
3. Recording tier-2 victories in `progression.js` `checkRunTerminalState`
4. Adding a normal-flow integration test that drives tier-2 victories via `checkRunTerminalState` (not manual `unlockQuestTier` on prereqs)

Verification mode is `code` — all acceptance criteria concern server state, helpers, and test behavior rather than on-screen rendering.
