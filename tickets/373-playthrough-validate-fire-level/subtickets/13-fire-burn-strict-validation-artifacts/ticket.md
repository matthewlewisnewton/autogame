# Fire burn strict validation artifacts

Regenerate the full `game/validation/fire/` artifact tree so ember-burn validation proves ticking HP loss, not merely a `burningUntil` flag. Harness strict-probe wiring (`runEmberBurnStep`, `buildAssertions`, `renderEmberBurnSection`, `checkEmberBurnConsistency`) should already be present from the retired 10/11 work — this pass lands the artifacts that prove it end-to-end.

## Acceptance Criteria

- Implementer's **first action** is `cd game && pnpm validate:fire`. Do not edit `harness/` before running unless `pnpm install` is required or validate:fire fails with a blocking harness error.
- `game/validation/fire/run-summary.json` `emberBurn` block includes `burnTickDamageApplied: true`, `hpDelta < 0`, `debugGodmodeOff: true`, and strict `emberBurnApplied: true` (not the stale `emberBurnApplied: true` + `hpDelta: 0` pair).
- `game/validation/fire/findings.md` Ember burn section includes separate `burnTickDamageApplied: PASS` and `emberBurnApplied: PASS` lines with a negative HP delta (no contradiction).
- `game/validation/fire/run-summary.json` `assertions.emberBurnApplied` is `true` only when `emberBurn.burnTickDamageApplied === true`.
- `cd game && pnpm validate:fire:check` (runs `harness/validate/verify-fire-artifacts.mjs`) exits `0` on the regenerated tree.
- All screenshots and JSON under `game/validation/fire/` are produced by the npm script — no hand-authored PNGs or JSON.
- If `validate:fire` still reports `hpDelta: 0` after sub-ticket 12, apply the **minimal** harness fix in `harness/validate/lib/cardMechanics.mjs` (e.g. extend post-screenshot tick poll past `BURN_TICK_INTERVAL_MS` arming delay) and re-run until artifacts pass or `findings.md` documents the real failure with screenshot evidence.
- `cd game && pnpm test:quick` passes.

## Technical Specs

- **Execute:** `cd game && pnpm validate:fire` then `cd game && pnpm validate:fire:check`.
- **Writable output (primary):** `game/validation/fire/**` — `run-summary.json`, `probes.json`, `findings.md`, `04-ember-burn.png`, and sibling screenshots/logs regenerated by the driver.
- **Read-only (verify present, edit only if validate:fire still fails):** `harness/validate/lib/cardMechanics.mjs` (`runEmberBurnStep` strict return shape), `harness/validate/playthrough.mjs` (`buildAssertions` derives `emberBurnApplied` from `burnTickDamageApplied`), `harness/validate/lib/findings.mjs` (`renderEmberBurnSection` `burnTickDamageApplied` line), `harness/validate/verify-fire-artifacts.mjs` (`checkEmberBurnConsistency`).
- **Depends on:** passed sub-ticket `12-ember-burn-godmode-sync-and-tick-damage` (godmode off + burn ticks deal damage at runtime).
- **Forbidden:** hand-editing artifact JSON/PNG; faking green when burn ticks deal zero damage.

## Verification: code
