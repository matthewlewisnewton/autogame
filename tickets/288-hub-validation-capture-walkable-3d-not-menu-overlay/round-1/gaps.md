1. Hub overlay dismissal was implemented by editing production client code, violating the ticket's explicit "No gameplay changes" scope (validation driver + outputs only). The change adds an always-on MutationObserver, an unnamespaced sticky global `window.__testKeepLobbyDismissed`, and a `withLobbyGuard` wrapper around seven hide sites — test machinery in the shipping client, with a latent footgun (if anything ever sets the global or hides #lobby outside the guard, the lobby silently never re-shows).
   Files: game/client/main.js, harness/validate/lib/multiPlayer.mjs, harness/validate/playthrough.mjs
   Fix: Revert all of the main.js changes (`MutationObserver`, `withLobbyGuard`, `_gameModifyingLobby`, the `__testKeepLobbyDismissed` guard in `showGameLobby`). Make `dismissLobbyOverlay()` own the sticky hide entirely in the harness — inject a validation-only `#lobby { display: none !important }` rule via `page.addStyleTag`/`page.addInitScript` before the hub screenshots; `!important` overrides the class toggle so the ~20Hz STATE_UPDATE re-show no longer fights it. Re-run the hub validation and confirm screenshots 01–04 still show the walkable 3D hub with the menu dismissed and both party-mates visible.
