1. No capture anywhere shows the boss-encounter HUD (warden name + draining HP bar) — the sole AC. Top-level and sub-ticket captures are generic fallback smokes that never reach Frost Crossing (`bossEncounter: null` in all probes).
   Files: harness/validate/presets/ice.mjs, harness/validate/playthrough.mjs, game/validation/ice/
   Fix: Actually run the retargeted ice validation (`pnpm validate:ice`) to drive the stage_boss flow and capture `06-boss-active.png` showing the active Permafrost Warden HUD with a draining HP bar; ensure the capture reaches the engaged (`encounter.phase=active`, `locked=true`) state via the debug-scenario path (`frost-crossing-encounter-trigger` / `frost-crossing-last-enemy`).

2. Committed `game/validation/ice/` artifacts are STALE and contradict the new code. `run-summary.json` has `objectiveType: "defeat_enemies"`, old assertion keys (`layoutDeployed`, `enemiesCleared`), zero `bossEncounter` probes, and old PNG names (`07-objective-complete.png`, `08-victory.png`). The new `verify-ice-artifacts.mjs` requires `bossSpawned`/`encounterActivated`/`bossDefeated`/`bossEncounterUiVisible` and the renamed PNGs, and fails if `layoutDeployed`/`enemiesCleared` are present — so verification against the tree as committed would fail.
   Files: game/validation/ice/run-summary.json, game/validation/ice/*.png, harness/validate/verify-ice-artifacts.mjs
   Fix: Regenerate `game/validation/ice/` from the retargeted ice preset (boss-encounter probes, `05-boss-dormant.png` / `06-boss-active.png` / `09-boss-defeated.png` / `10-victory.png`, etc.) and commit the refreshed run-summary + PNGs so they match the new preset and pass `verify-ice-artifacts.mjs`.
