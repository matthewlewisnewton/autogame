# 251-enemy-display-metadata

## Difficulty: easy

## Goal

Add a display name + short description (and which stats to surface) to each enemy type/variant in ENEMY_DEFS. Prerequisite for the lock-on info panel.

## Acceptance Criteria

- Each enemy type/variant has a display name + 1-line description + surfaced-stats list. Test.

## Verification

Run the harness checks (vitest server+client) and verify the acceptance criteria above.
