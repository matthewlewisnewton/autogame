# 253-level2-tier-framework-unlock

## Difficulty: hard

## Goal

Foundation: a level/quest can have a Tier-2 variant, unlock-gated behind clearing Tier-1 (persistent per-account unlock + quest/run/selection plumbing). No level-specific content here.

## Acceptance Criteria

- A quest exposes a Tier-2 that is unlockable only after beating Tier-1; unlock persists; selection UI surfaces it once unlocked. Tests for unlock gating.

## Verification

Run the harness checks (vitest server+client) and verify the acceptance criteria above.
