1. Game server will not start in harness capture or default dev — `initAuth()` throws at `auth.js:77` because no launch path sets `ALLOW_DEV_AUTH=1` or `JWT_SECRET`.
   Files: `game/server/package.json`, `harness/steps/game.py`, `game/client/scripts/test-deck-loadout.mjs`, `game/client/scripts/test-card-evolution.mjs`, `game/client/scripts/test-quest-completion.mjs`, `game/client/scripts/test-telepipe-suspend-resume.mjs`, `game/client/scripts/test-world-stage-transition.mjs`
   Fix: Add `ALLOW_DEV_AUTH=1` to every non-test server launch env (e.g. `"dev": "ALLOW_DEV_AUTH=1 nodemon index.js"` in `game/server/package.json`; extend the `env={...}` dict in `harness/steps/game.py` `start_game`; add `ALLOW_DEV_AUTH: '1'` alongside existing `ALLOW_DEBUG_SCENARIOS` in smoke scripts). Re-run capture to confirm `metrics.json` `ok: true`.
