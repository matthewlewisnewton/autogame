`LOGBOOK.md` is resolved.

**What conflicted:** Both sides appended new review entries after v0.217 — `main` had v0.220 (229-hub-stage-layout-server) and v0.221 (272-socketHandlers-extract-lobby); the branch had v0.222 (264-admin-character-roster-view).

**Resolution:** Combined all three in version order:
1. **v0.220** — hub stage layout (from `main`)
2. **v0.221** — socket handler extraction (from `main`)
3. **v0.222** — admin character roster view (from the ticket branch)

Conflict markers are removed; the file ends with the existing `## Remaining gaps / None.` footer. The harness can commit and finish the merge.
