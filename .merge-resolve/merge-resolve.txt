`LOGBOOK.md` is resolved with both sides kept in version order:

1. **v0.149** (from `main`) — dodge-roll cleanup: summary bullets, debug-scenario note about `sloped-dungeon` in round-3 capture, and remaining-gaps closure.
2. **v0.150** (from the ticket branch) — loot-magnet cleanup: debug-scenario note, capture-limitations section for ring VFX, and its remaining-gaps closure.

All conflict markers are removed. The harness can continue the merge.
