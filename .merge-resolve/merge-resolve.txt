Resolved `LOGBOOK.md` by combining both sides in version order:

1. **v0.290** — chain-lightning card (from `main`)
2. **v0.291** — rare medic enemy (from `main`)
3. **v0.292** — burning/slow mutual exclusion (from this ticket)

Each entry keeps its own **Remaining gaps** section, matching the structure on both branches. All conflict markers are removed; the harness can finish the merge.
