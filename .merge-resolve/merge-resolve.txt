All three conflicted files are resolved with both sides preserved:

**`game/client/test/vfx-primitives.test.js`** — Combined imports: Mirror Ward primitives (`spawnMirrorWardShellEffect`, `dismissMirrorWardShellEffect`, `spawnMirrorWardReflectBurst`) from the ticket plus `spawnInfernoPillarEffect` from main. Both test suites were already present below the import block.

**`LOGBOOK.md`** — Kept all three version entries in chronological order: v0.329 (fireball), v0.331 (thermal column), then v0.332 (mirror ward).

**`TASKS.md`** — Listed both cleanup tickets: `364-cleanup-anim-thermal-column` and `367-cleanup-anim-mirror-ward`.

All conflict markers are removed. The `vfx-primitives.test.js` suite passes locally. The harness can commit and complete the merge.
