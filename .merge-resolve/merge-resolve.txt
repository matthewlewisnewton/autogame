**`LOGBOOK.md` is resolved** — all conflict markers are gone.

The conflict was two independent log appendages after `v0.190`:

| Side | Content |
|------|---------|
| **Branch (HEAD)** | `v0.197` — 215-lobby-atomic-hat-unlock-ordering (atomic hat unlock / persistence ordering) |
| **main** | `v0.196` — 217-lobby-explicit-phase-state-machine and `v0.195` — 219-input-unify-keyboard-onto-keymap |

**Resolution:** kept all three entries, ordered **newest-last** per the logbook header (`v0.195` → `v0.196` → `v0.197`), with the shared closing `## Remaining gaps` / `None.` at the end.

No other conflicted files had markers in this worktree. The harness can finish the merge and run tests.
