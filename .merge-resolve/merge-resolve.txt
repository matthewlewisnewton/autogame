Merge conflicts are resolved in both files.

### `game/server/simulation.js`
Combined the config imports from both sides:
- **Ticket (270):** `DIFFICULTY_ENEMY_DAMAGE_PER_PLAYER`, `difficultyScaleFactor`, `runPlayerCount` — used for live party-size enemy damage scaling at strike resolution
- **Main:** `SPIRE_EDGE_HAZARD_DAMAGE`, `SPIRE_EDGE_HAZARD_COOLDOWN_MS` — used by the spire edge hazard logic already present in the file

### `LOGBOOK.md`
Kept all three review entries in chronological order:
1. **v0.237** — 249-rooms-distinctness-identity (main)
2. **v0.238** — 246-spire-tower-identity (main)
3. **v0.240** — 270-difficulty-scale-with-player-count (ticket)

All conflict markers are removed. The targeted test run passed (71 files, 1393 tests). The harness can proceed with commit and merge completion.
