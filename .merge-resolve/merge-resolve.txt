`LOGBOOK.md` is resolved. The conflict was at the end of the logbook: the branch added the **v0.200** entry for ticket 218, while `main` had added **v0.199** (220) and **v0.198** (222) after the branch diverged.

The merged file keeps all three entries in descending version order:

1. **v0.200** — `218-lobby-handler-preamble-and-session-helper` (branch)
2. **v0.199** — `220-input-rebind-collision-guard` (`main`)
3. **v0.198** — `222-data-collapse-card-def-drift` (`main`)

All conflict markers are removed. No other conflicted files remain in the worktree. The harness can proceed with commit and tests.
