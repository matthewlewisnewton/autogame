Merge conflicts are resolved. Both tickets’ behavior is preserved:

**`game/shared/theme.json`** — All six stage-boss objective strings: trial warden (existing), summit warden (262), and canyon warden (261).

**`game/client/questBoard.js` & `game/server/quests.js`** — `spire_ascent` uses summit warden copy via early return; `canyon_descent` and default (`arena_trials`) use the canyon/trial warden branching from main.

**`game/client/test/questBoard.test.js`** — Both test blocks kept: spire ascent summit warden and canyon descent canyon warden.

**`LOGBOOK.md`** — Both release entries kept in version order: v0.270 (spire ascent) then v0.269 (sunken canyon).

All conflict markers are removed. `questBoard.test.js` and `quests.test.js` pass (25 tests). The harness can commit and finish the merge.
