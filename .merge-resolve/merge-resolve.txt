Merge conflicts are resolved. Summary:

**`TASKS.md`** — Kept both cleanup tickets from each side:
- `278-cleanup-booth-deck-terminal` (from `main`)
- `279-cleanup-booth-quest-counter` (from this branch)

**`game/client/main.js`** — Combined both intents:
- **From `main`:** Deck/shop test hooks (`__openDeckBoothForTest`, `__openShopBoothForTest`, `__requestDebugBoothOpenForTest`, `__requestDebugShopBoothOpenForTest`)
- **From this branch:** Generalized `?booth=<id>` comment and `requestBoothDebugOpen()` support for both `character` and `quest` (opening `openQuestPanel()` for quest)

The rest of the file was already merged (quest booth wiring, deck/shop imports, `window.__requestBoothDebugOpenForTest` at the bottom). All conflict markers are removed; the harness can commit and run tests.
