Both conflicts resolved, no markers remain.

**LOGBOOK.md** — Both sides appended new completion entries. Kept all of main's (`v0.153`–`v0.158`) and the branch's (`v0.159`), ordering `v0.159` last to preserve the "appended newest-last" version-monotonic convention.

**TASKS.md** — Two conflicts:
1. Status flags: main marked `143` done, the branch marked `144` (this ticket) done. Kept both as `[x]` so neither completion is lost.
2. Backlog tail: both sides added a different `200-…` entry. Kept both as separate backlog items.

No unrelated changes; both files are marker-free and consistent.
