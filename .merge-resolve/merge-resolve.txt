Merge conflicts are resolved. Each conflict was the same pattern: both branches added a new enemy/feature at the same registry location, so I kept **both** sides rather than choosing one.

**Resolved files:**

| File | Resolution |
|------|------------|
| `game/client/renderer.js` | Both `glacial_thrower` and `ember_wraith` in `ENEMY_GEOMETRY` and `ENEMY_ATTACK_VISUAL` |
| `game/server/simulation.js` | Both enemy defs in `ENEMY_DEFS` |
| `game/server/debugScenarios.js` | Both `glacial-thrower` and `ember-wraith` debug scenario branches |
| `game/server/index.js` | Both `glacial-thrower` and `ice-ball-ready` in `DEBUG_SCENARIOS` |
| `game/server/test/enemy_display_catalog.test.js` | Both enemy types in `ENEMY_TYPES` |

All conflict markers are removed. Quick test run: **170 files, 2632 tests passed**. The harness can commit and finish the merge.
