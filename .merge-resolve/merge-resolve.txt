Merge conflicts are resolved. Summary:

### `game/client/main.js`
Kept the branch’s `launchBooth.js` import alongside the character-booth imports already merged from `main` (`characterBooth.js`, `cosmeticForm.js`, etc.). Both booth flows stay wired in one file.

### `LOGBOOK.md`
Combined both review sections in version order:
- **v0.242** — 237-booth-mission-launch (launch ready-up)
- **v0.241** — 239-booth-character-editor (character editor booth)

Single “Remaining gaps: None.” at the end.

All conflict markers are removed. `launchBooth.test.js` (9) and `characterBooth.test.js` (8) pass. The harness can commit and finish the merge.
