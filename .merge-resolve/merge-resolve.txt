Merge conflicts are resolved in both files.

### `TASKS.md`
Kept **both** backlog entries (main’s character-models spike and this ticket’s follow-up cleanup):
- `197-cleanup-character-models-spike-base-player-model` (from `main`)
- `198-cleanup-cleanup-key-item-flare-beacon` (from this branch)

They sit after the existing `197-cleanup-models-wire-enemy-placeholders` line.

### `LOGBOOK.md`
Appended **both** version sections in timestamp order (matching the file’s v0.144 → v0.146 pattern):
1. **v0.147** — character models spike review (`main`)
2. **v0.148** — flare beacon cleanup review (this ticket)

All conflict markers are removed. The harness can continue the merge and run tests.
