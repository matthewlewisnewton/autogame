`LOGBOOK.md` is resolved. The conflict was two parallel version histories: `main` had landed v0.291 (Field Medic) and v0.292 (burn/slow mutual exclusion) while this branch added v0.293 (Vault Wyrm burning rebalance).

**Resolution:** Combined all three entries in chronological version order after the existing v0.290 chain-lightning entry:

1. **v0.291** — Field Medic enemy (from `main`)
2. **v0.292** — Burning/slow mutual exclusion (from `main`)
3. **v0.293** — Vault Wyrm burning breath rebalance (from this ticket)

Each entry keeps its own review sections and `## Remaining gaps` footer, matching the LOGBOOK pattern used by prior entries. All conflict markers are removed; the harness can proceed with the merge commit.
