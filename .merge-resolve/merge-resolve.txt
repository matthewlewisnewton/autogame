Merge conflicts are resolved in both files.

### `TASKS.md`
Kept **both** cleanup backlog entries:
- `279-cleanup-plaza-arena-identity` (this ticket)
- `279-cleanup-booth-quest-counter` (landed on main)

### `LOGBOOK.md`
Combined both sides in version order:
- **v0.246–v0.249** from main (booth shop, quest counter, hub polish, paid appearance change)
- **v0.250** from this branch (`247-plaza-arena-identity` review entry)

All conflict markers are removed. The harness can commit and finish the merge.
