`LOGBOOK.md` is resolved — all conflict markers are gone.

**What was conflicting:** Both sides appended a new version entry after v0.261. `main` had **v0.264 — 241-free-hat-swap**; the branch had **v0.265 — 256-level2-sunken-canyon**.

**Resolution:** Kept both entries in chronological order (v0.264, then v0.265), each with its own full content and `## Remaining gaps / None.` section, matching the existing logbook pattern.

The harness can proceed with the merge commit and test run.
