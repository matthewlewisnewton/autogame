All merge conflict markers are resolved. Summary of how the two sides were combined:

### LOGBOOK.md
Kept the v0.337 fire-validation entry and the main-side entries (v0.331 thermal column, v0.332 mirror ward, v0.333 spike trap, v0.336 sunken canyon revalidation).

### game/client/main.js
- Scenario apply: card-probe hand refresh **and** rotation alignment via `alignAttackFacing`
- Harness probes: main’s `slowActive`/`burnActive`/windup fields **plus** ticket’s top-level `windupFlashing`
- Enemy probes: main’s status fields **plus** ticket’s `frozenUntil`

### game/server/debugScenarios.js
- Both `fire-telepipe-ready` and `fireball-hand-ready` as separate scenarios
- All ember-descent fire scenarios retained
- Canyon tier-2 handler accepts `canyon-descent-tier-2` **or** `canyon-descent-telepipe-ready`

### harness/validate/lib/telepipe.mjs
- Main’s `VITALS_MS_REGEN_TICKS = 800` and `probesMatchCardChargesReset` / `probesMatchFreshDeployWithMsReset` split

### harness/validate/lib/findings.mjs
- Fire sections (ember burn, card mechanics, telepipe reset, stage-boss gap) **and** canyon sections (boss UI, visual identity, slow/burn, purifying pulse, windup, canyon telepipe, new-content exercises)
- Fire-aware floor alignment with preset/objectiveType params

### harness/validate/playthrough.mjs
- Imports and drivers for both fire (`cardMechanics.mjs`) and canyon (`cardExercise.mjs`, `runCanyonTelepipeNewSortieStep`)
- `runDefeatEnemiesCombatStep` for fire **and** `runSunkenCanyonMidCombatProbeStep` for canyon
- Boss encounter routing: defeat-enemies for fire, sunken-canyon full pipeline when applicable, standard boss encounter otherwise
- Probes/screenshots include both `telepipeReset` (fire) and `cardExercises`/`canyonTelepipe` (canyon)

All 2909 tests pass (coverage threshold failure is unrelated to the merge). The harness can stage and complete the merge commit.
