All three conflicted files are marker-free. Summary of how each was merged:

### `game/server/index.js`
Combined both sides in `runGameLoopTick()`:
- **From main:** `try/catch` around each lobby tick (server resilience from booth/deck tickets)
- **From branch:** `syncAndEmitHubPresenceIfChanged(io, lobby)` in the lobby-phase branch (hub presence broadcast)

### `TASKS.md`
Kept all three cleanup backlog entries:
- `278-cleanup-hub-shared-presence` (branch)
- `278-cleanup-booth-mission-launch` (main)
- `278-cleanup-booth-deck-terminal` (main)

### `LOGBOOK.md`
Merged review entries in version order (newest first):
- v0.244 — hub-shared-presence (branch)
- v0.243 — booth-deck-terminal (main)
- v0.242 — booth-mission-launch (main)
- v0.241 — booth-character-editor (main)
- v0.240 — difficulty-scale-with-player-count (main)

`index.js` passes syntax check. The harness can commit and run the full test suite.
