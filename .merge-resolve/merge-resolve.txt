Merge conflicts are resolved. Summary of how each side was combined:

### `game/client/dungeon.js`
- Kept spire-ascent tier/ramp material functions (summit tints, beacon, edge hazards)
- Dropped duplicate `materialColorHex` and legacy `roleFloorMaterials` (superseded by main's profile palette)
- Room loop now uses spire tier materials when `profile === 'spire-ascent'`, otherwise main's `roleFloors` / `profileFloorMaterial` / `profileWallMaterial`

### `game/client/test/dungeon.test.js`
- Merged imports from both branches
- Updated the non-spire material test to expect crowded profile materials (main's default when no profile is set) instead of legacy `floorMaterial`

### `game/server/debugScenarios.js`
- Kept all three spire debug scenarios (`spire-summit-beacon`, `spire-mid-tier-hazard`) from the ticket branch
- Added `open-verticality` from main

### `LOGBOOK.md`
- Kept main's v0.234, v0.236, and v0.237 entries
- Appended v0.238 (spire-tower-identity) at the end

All conflict markers are removed. Dungeon-related tests pass (206/206). The harness can commit and complete the merge.
