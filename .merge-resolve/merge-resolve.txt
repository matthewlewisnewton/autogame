`LOGBOOK.md` is resolved and marker-free.

**What was conflicting:** Both sides appended new version entries after v0.299 — `main` added v0.302 (305) and v0.303 (303); the ticket branch added v0.304 (289).

**Resolution:** Kept all three entries in ascending version order, matching the existing logbook convention:

1. **v0.302** — 305-recapture-walkable-hub-after-overlay-fix (from `main`)
2. **v0.303** — 303-card-balance-analysis (from `main`)
3. **v0.304** — 289-card-charges-persist-on-telepipe-resume-reset-on-new-sortie (from the ticket branch)

Each entry retains its full review content and its own "Remaining gaps" section. The duplicate trailing section after the conflict markers was removed. Ready for the harness to commit and complete the merge.
