All four conflicted files are resolved. Summary:

**`game/client/cardRenderers.js`** — Combined config imports so both tickets' constants are available:
- `ATTACK_EFFECT_DURATION` (permafrost lance timing)
- `PHOTON_BARRAGE_SWING_DELAY_MS` (excalibur photon barrage)
- `MINION_SUMMON_IN_MS` (shared)

**`game/client/test/cardRenderers.test.js`** — Same combined import for `ATTACK_EFFECT_DURATION` and `PHOTON_BARRAGE_SWING_DELAY_MS`.

**`game/client/test/vfx-primitives.test.js`** — Kept all three renderer imports:
- `spawnAttackEffect` (permafrost lance primitive test)
- `spawnDivineGraceEffect` / `spawnDivineGraceColumn` (sanctum pulse tests)

**`LOGBOOK.md`** — Merged review entries in version order: v0.326 (excalibur photon), v0.327 (sanctum pulse), v0.328 (permafrost lance), each with its own "Remaining gaps" section.

No conflict markers remain in the target files. Client test suite passes (60 files, 949 tests). The harness can commit and complete the merge.
