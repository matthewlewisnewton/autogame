Merge conflict in `LOGBOOK.md` is resolved.

**What was conflicting:** Both sides appended a new version entry at the end of the logbook — `main` had **v0.212** (268-scaling-thread-lobby-state-explicitly) and the ticket branch had **v0.213** (265-sec-debug-gate-no-header-spoof).

**Resolution:** Kept both entries in ascending version order:
1. **v0.212** — scaling thread lobby state migration review (from `main`)
2. **v0.213** — debug gate header-spoof security fix review (from the ticket branch)

All conflict markers are removed; the file is consistent with the rest of the logbook. The harness can proceed with commit and merge.
