Merge conflicts are resolved in both files.

### `LOGBOOK.md`
Kept **both** review entries in version order:
- **v0.202** — `216-lobby-remove-dead-hostid` (from `main`)
- **v0.203** — `228-gameplay-card-cinder-snare` (from the ticket branch)

### `game/server/test/guard_block.test.js`
Kept the ticket’s **drift-tolerant** cooldown assertions (250ms window, matching the `blockingUntil` test below), with the clearer comment explaining why bounds are used instead of an exact value. Dropped the duplicate `toBeGreaterThan(0)` because line 354 already asserts it. Did not keep `main`’s tighter 50ms window, since that conflicts with the ticket’s timing-determinism work.

All conflict markers are removed. Server tests passed (1214 tests). The harness can commit and finish the merge.
