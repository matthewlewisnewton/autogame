`LOGBOOK.md` is resolved. Both sides were independent log entries that landed at the same spot after `v0.222`, so I kept **both** in ascending version order:

1. **v0.224 — 273-socketHandlers-extract-deck** (from `main`) — deck handler extraction review
2. **v0.225 — 230-hub-client-render** (this ticket) — hub client render / transparent lobby overlay review

All conflict markers are removed. The file follows the existing logbook pattern (newer versions toward the bottom). No other conflicted files were listed. The harness can commit and finish the merge.
