Merge conflicts are resolved. Every conflict was the same pattern: this branch added `arena_champion` (plaza Tier-2 boss) while `main` had landed `spire_warden` (spire Tier-2 boss). Both are kept everywhere.

**Resolved files:**

| File | Resolution |
|------|------------|
| `game/client/models.js` | Both model registry entries |
| `game/client/renderer.js` | Both `ENEMY_GEOMETRY` and `ENEMY_ATTACK_VISUAL` entries |
| `game/client/test/models-registry.test.js` | Both paths in `ENTITY_MODEL_PATHS` |
| `game/server/config.js` | Both card drops and MS drops |
| `game/server/simulation.js` | Both `ENEMY_DEFS` entries as separate objects |
| `game/server/test/enemy_display_catalog.test.js` | `ENEMY_TYPES` includes both bosses |
| `LOGBOOK.md` | v0.271 (plaza), v0.270 (spire), and v0.269 (canyon) entries in order |

All conflict markers are removed. Focused tests on the merged enemy types pass (35/35). The harness can commit and run the full suite.
