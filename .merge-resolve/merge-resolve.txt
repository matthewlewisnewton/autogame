Resolved the `LOGBOOK.md` conflict by keeping **both** review entries:

1. **v0.199 — 220-input-rebind-collision-guard** (this ticket’s input rebind collision guard review)
2. **v0.198 — 222-data-collapse-card-def-drift** (card def drift work that landed on `main`)

They’re ordered by version number (newest first), matching the logbook pattern. All conflict markers are removed — the harness can proceed with the merge.
