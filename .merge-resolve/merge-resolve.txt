Merge conflicts are resolved in both files.

**`TASKS.md`** — Kept both cleanup backlog entries (main’s `224-cleanup-data-derive-shop-pool-from-defs` and this ticket’s `226-cleanup-data-centralize-enemy-construction-and-consts`), in ticket-number order.

**`LOGBOOK.md`** — Kept both version entries in descending version order:
- **v0.191** — ticket 225 (enemy construction centralization), with its own “Remaining gaps” section
- **v0.190** — ticket 223 (shop pool derived from card defs), with its own “Remaining gaps” section

All conflict markers are removed. The harness can continue the merge/rebase.
