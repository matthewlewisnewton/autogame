`LOGBOOK.md` is resolved and marker-free.

**What conflicted:** At the end of the file, after the v0.228 (`211-net-slim-per-tick-state-broadcast`) entry. Main had no further content; this branch added the v0.230 (`231-hub-lobby-phase-movement`) review entry.

**Resolution:** Kept both sides:
- **Main:** v0.229 (`275-socketHandlers-extract-keyitem`) and v0.228 entries already present above the conflict
- **Branch:** Appended the full v0.230 lobby-phase movement logbook entry after v0.228

The file now ends with the ticket’s review notes and has no remaining `<<<<<<<`, `=======`, or `>>>>>>>` markers. The harness can commit and finish the merge.
