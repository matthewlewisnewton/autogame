Merge conflict in `LOGBOOK.md` is resolved.

**Resolution:** Both ticket log entries are kept in descending version order (matching the rest of the logbook):

1. **v0.294 — 297-fireball-card-inflicts-burning** (this ticket) — full fireball card review, including its `## Remaining gaps` section
2. **v0.293 — 298-vault-wyrm-burning-rebalance** (from `main`) — full Vault Wyrm burning rebalance review, including its `## Remaining gaps` section

All conflict markers are removed. The harness can proceed with the merge commit.
