Merge conflicts are resolved in both files.

### `TASKS.md`
Combined cleanup backlog entries from both sides:
- All 9 cleanup tickets from `main` (hub presence, booth terminals, plaza arena, etc.)
- The branch’s `278-cleanup-sec-jwt-require-explicit-dev-auth` entry

### `LOGBOOK.md`
- Kept all `main` logbook entries (`v0.236` through `v0.269`) after the shared `v0.234` section
- Appended the ticket’s `v0.273 — 266-sec-jwt-require-explicit-dev-auth` entry at the end
- Restored the truncated branch entry (it had started mid-sentence with “throw messages.”) using the round-9 review content so the logbook entry is complete and coherent

No conflict markers remain in either file. The harness can proceed with commit and merge.
