`LOGBOOK.md` is resolved. Both sides are kept in ascending version order:

1. **v0.261 — 255-level2-rooms** (from `main`) — rigid layout mode, Tier 2 quest wiring, debug scenarios, and test coverage
2. **v0.264 — 241-free-hat-swap** (from this branch) — hat equip unlock checks, `?booth=hatswap` debug hook, and design consistency review

All conflict markers are removed. The file ends with the v0.264 “Remaining gaps: None.” section, consistent with each ticket’s standalone logbook entry. The harness can proceed with the merge.
