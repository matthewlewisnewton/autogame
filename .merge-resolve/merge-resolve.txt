`LOGBOOK.md` is resolved.

**Resolution:** Both logbook entries are kept in chronological order:

1. **v0.306** (from `main`) — restore HP healing cards, keep no auto-level heal  
2. **v0.307** (from the branch) — card wind-up commitment input-lock mechanic  

The file follows the existing LOGBOOK pattern (ascending version numbers, newest at the end) and closes with a single `## Remaining gaps` section. All conflict markers are removed; no other files had conflicts.
