Merge conflicts are resolved. Summary:

### `TASKS.md`
Kept both cleanup backlog entries: `274-cleanup-telepipe-hub-suspend-resume-integration` (from `main`) and `277-cleanup-socketHandlers-extract-run-and-cleanup` (from the ticket branch).

### `LOGBOOK.md`
Merged chronologically: `main` entries for v0.230–v0.232, then the ticket’s v0.233 entry for `276-socketHandlers-extract-run-and-cleanup`.

### `lobbyHandlers.js`
Kept the ticket’s extraction: run-lifecycle handlers stay delegated to `runHandlers.register()` with no duplicate inline handlers or `isPlayingPhase`/`isLobbyPhase` imports in this file.

### `runHandlers.js` (coherence fix)
Ported `main`’s lobby-phase movement logic into the extracted `move` handler so hub movement still works after the refactor:
- Accept moves in both playing and lobby phases
- Scope dead/extracted checks to playing phase only

All conflict markers are removed. Server test suite: **1325/1325 passed** (including `lobby_hub_movement.test.js`). Ready for the harness to commit and complete the merge.
