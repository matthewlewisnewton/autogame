Merge conflicts are resolved in both files.

### `game/package.json`
Kept all three smoke scripts:
- `test:smoke:quest-completion` (ticket 176)
- `test:smoke:world-stage-transition` (main)
- `test:smoke:telepipe-suspend-resume` (main)

JSON validates; all three script files are present.

### `LOGBOOK.md`
Merged both sides in descending version order (matching the existing v0.170 / v0.169 pattern):
1. **v0.172** — 176-qa-quest-objective-completion (ticket)
2. **v0.171** — 175-qa-telepipe-suspend-resume (main)
3. **v0.168** — 168-bug-intermittent-stuck-lobby-socket (main)

No conflict markers remain. The harness can commit and finish the merge.
