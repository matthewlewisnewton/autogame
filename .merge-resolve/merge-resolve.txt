Merge conflict in `game/server/index.js` is resolved. Both sides were additive debug-scenario registry entries, so I kept all of them:

1. **`DEBUG_SCENARIOS`** — ticket entries plus main:
   - `field-medic`, `field-medic-spawn` (ticket)
   - `chain-lightning-ready` (main)

2. **`DEBUG_SCENARIOS_WITHOUT_DEFAULT_SPAWN`** — ticket entries plus main:
   - `field-medic`, `field-medic-spawn` (ticket)
   - `slippery-floor-lab`, `frost-crossing-tier-1` (main)

All conflict markers are removed; the file is ready for the harness to commit and run tests.
