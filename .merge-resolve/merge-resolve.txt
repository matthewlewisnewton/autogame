Merge conflicts are resolved. Summary:

### `game/client/cardRenderers.js`
- Combined imports: kept `PHOTON_BARRAGE_SWING_DELAY_MS` from `main` alongside the fireball branch’s existing config imports.

### `game/client/test/cardRenderers.test.js`
- Removed duplicate `ATTACK_EFFECT_DURATION` import (already imported on line 3 with `PHOTON_BARRAGE_SWING_DELAY_MS`).
- Kept **both** tests from the conflict: Excalibur Photon greatslash stagger test (branch) and heavy-greatsword “no photon trail/telegraph” test (`main`).
- Adjusted two `excalibur_photon` tests to call `ctx.runScheduled()` — needed because the fireball branch’s auto-merged `makeCtx` defers `scheduleAfter` callbacks (for fireball impact timing), while `main`’s excalibur tests assumed immediate execution.

### `LOGBOOK.md`
- Merged all four version entries in order: v0.326 (Excalibur Photon), v0.327 (Sanctum Pulse), v0.328 (Permafrost Lance), v0.329 (Fireball), each with its own “Remaining gaps” section.

All conflict markers are removed. `cardRenderers.test.js` passes (147/147). The harness can proceed with commit and merge.
