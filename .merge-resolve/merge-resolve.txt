Merge conflicts are resolved. Both tickets’ changes are preserved:

**`harness/validate/playthrough.mjs`** — `PRESET_MODULES` now registers all three presets: `rooms`, `open-plaza` (from main), and `sunken-canyon` (from this branch).

**`TASKS.md`** — Both cleanup tickets are listed:
- `284-cleanup-playthrough-validate-open-plaza`
- `284-cleanup-playthrough-validate-sunken-canyon`

**`LOGBOOK.md`** — Both review entries are kept in version order: v0.275 (open-plaza) then v0.276 (sunken-canyon), with a single trailing “Remaining gaps” section.

All conflict markers are removed. `playthrough.mjs --help` runs successfully. The harness can commit and finish the merge.
