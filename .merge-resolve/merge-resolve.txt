`LOGBOOK.md` is resolved. The conflict was at the end of the log where both sides added new ticket entries.

**Resolution:** Kept all three entries in ascending version order (matching the rest of the logbook):

1. **v0.313** — `311-slower-scaling-early-strong-cards-plus-astral-trim` (from `main`)
2. **v0.315** — `310-apply-3-optional-newcard-balance-tunes` (from `main`)
3. **v0.316** — `314-ether-scythe-evolution-gold-health-on-kill` (from this branch)

All conflict markers are removed. The file ends with the v0.316 "Remaining gaps: None." section. The harness can proceed with commit and merge.
