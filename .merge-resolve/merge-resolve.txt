Merge conflicts are resolved. Summary of how each file was combined:

### `game/client/cardRenderers.js`
Kept **both** constant sets:
- **318 (ticket):** `STORM_EAGLE_ARC_STYLE` for storm eagle lightning arcs
- **317 (main):** `ARCANE_FAMILIAR_*`, `MANA_LEACH_*`, and `SOUL_DRAIN_*` for arcane radial spell renderers

### `game/client/test/cardRenderers.test.js`
Combined registry expectations from both sides:
- **318:** `thunderbird` and `storm_eagle` each have **2** renderers (summon + attack VFX)
- **317:** `dragons_breath`, `inferno_pillar`, and `gravity_well` each have **1** renderer

### `LOGBOOK.md`
Kept both review entries in version order: **v0.323** (318) then **v0.322** (317).

All conflict markers are removed. `cardRenderers.test.js` passes (97/97 tests). The harness can commit and finish the merge.
