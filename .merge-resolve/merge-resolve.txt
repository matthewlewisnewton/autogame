`LOGBOOK.md` is resolved.

Both sides were kept and ordered by version (newest last, per the logbook convention):

1. **v0.285 — 295-fire-level** (from `main`) — fire cavern layout, debug scenarios, themed visuals
2. **v0.286 — 290-slow-status-effect-foundation** (from this ticket) — SLOW status helpers, movement scaling, client prediction, slow indicator

All conflict markers are removed. The file ends with the standard `## Remaining gaps / None.` section. The harness can commit and finish the merge.
