Merge conflicts are resolved in both files.

**`TASKS.md`** — Both follow-up cleanup tickets are listed (main’s survive-objective entry first, then this branch’s dedupe-card-defs entry).

**`LOGBOOK.md`** — Both log entries are kept in version order: **v0.175** (`174-gameplay-survive-objective` from main) then **v0.176** (`163-cleanup-dedupe-card-defs-server-client` from this ticket).

All conflict markers are removed. The harness can finish the merge and run tests.
