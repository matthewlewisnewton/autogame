Merge conflicts are resolved. Both tickets coexist:

**`game/server/enemyVariants.js`** — Registry includes `test`, `warded` (shield on apply), and `leeching` (`applyLeechHeal`).

**`game/server/debugScenarios.js` & `game/server/index.js`** — Both debug scenarios are wired:
- `warded-enemy` — spawns shielded warded grunt + plain grunt
- `variant-leeching` — spawns leeching grunt + plain grunt

**`game/client/renderer.js`** — Unified badge colors in `VARIANT_BADGE_COLORS` (`warded` cyan, `leeching` teal, default magenta). `variantMarkerColor()` delegates to `variantBadgeColor()` for backward compatibility. Marker creation uses the leeching branch’s `badgeColor` API with the hex-change guard.

**`LOGBOOK.md`** — Both review entries kept in order (v0.179 leeching, then v0.181 warded).

All conflict markers are removed. Tests: **75 files, 1690 tests passed**. The harness can commit and finish the merge.
