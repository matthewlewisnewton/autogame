`LOGBOOK.md` is resolved.

The conflict was two new logbook entries that both landed after v0.224:
- **main**: v0.225 — `230-hub-client-render` (hub client render)
- **branch**: v0.226 — `274-socketHandlers-extract-trade` (trade handler extraction)

Both entries are kept in version order (v0.225, then v0.226). All conflict markers are removed. The harness can finish the merge.
