`LOGBOOK.md` is resolved. The conflict was two new logbook entries appended after v0.299 — one from each side:

- **main (v0.302):** 305-recapture-walkable-hub-after-overlay-fix — hub validation harness and walkable presentation findings
- **branch (v0.303):** 303-card-balance-analysis — card balance metrics, safe tunings, and tests

The merged file keeps **both** entries in version order (v0.302 → v0.303), each with its full review sections and its own `## Remaining gaps / None.` footer, matching the pattern used elsewhere in the logbook.

All conflict markers are removed. The harness can proceed with the merge commit and test run.
