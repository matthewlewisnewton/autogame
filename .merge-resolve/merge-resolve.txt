Merge conflicts are resolved. Summary:

### `game/server/index.js`
Kept the ticket’s extraction pattern: build `ctx`, call `lobbyHandlers.register(socket, ctx)`, then run resume/reconnect logic. Removed main’s inline socket handlers (they belong in the extracted module).

Added `emitQuestPayloadToLobby` to `ctx` so quest-tier payloads from main can flow through the extracted handlers.

### `LOGBOOK.md`
Combined both sides chronologically: main entries (v0.215–v0.220) followed by the ticket entry (v0.221).

### `game/server/socketHandlers/lobbyHandlers.js` (supporting change)
Main’s quest-tier behavior (`createLobby`, `selectQuest`, `playerReady`) was only in the conflicted inline handlers. Ported that logic into `lobbyHandlers` so extraction and tier gating coexist. Tier gating tests pass.

All conflict markers are removed; syntax checks and targeted tests (`quest_tier_gating`, `quest_tier_lobby_sync`, `lobbies`) pass. Ready for the harness to commit and run the full suite.
