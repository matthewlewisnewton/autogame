All conflict markers are removed. Summary of how each file was merged:

### Code
- **`game/client/main.js`** — Kept ticket 288’s `if (lobbyEl)` null guard and main’s `setLobbyHudVisible(false)` (overlay-fix from #304).
- **`harness/validate/playthrough.mjs`** — Combined both flows:
  - Proximity assertion + `dismissLobbyOverlay()` before screenshots (288)
  - `requireWalkableHubPresentation()` probes and `walkablePresentation` in the return value (305/main)

### Validation artifacts
- **`findings.md`**, **`probes.json`**, **`run-summary.json`** — Merged:
  - Ticket 288 hub-walk run metadata (lobby names, ports)
  - Main telepipe vitals (`telepipeVitalsPreserved`) and walkable-presentation probes
- **PNG screenshots** — Kept ticket 288’s regenerated captures (overlay dismissed)

### Docs
- **`LOGBOOK.md`** — Prepended v0.305 (288) entry before main’s v0.287–v0.304 entries

The harness can commit and finish the merge when ready.
