Merge conflicts are resolved in both files.

### `game/server/dungeon.js`
Kept **both** feature sets in order:
1. **Ticket (254):** `OPEN_PLAZA_RIGID_HAZARDS` and `placeOpenPlazaHazardsRigid()` for deterministic open-plaza rigid layout mode
2. **Main:** `buildSunkenCanyonCliffLips()` and `buildSunkenCanyonCliffHazards()` for sunken-canyon cliff lips and plateau hazard bands

Removed the stray extra `}` that came from the conflict. `node --check` passes.

### `LOGBOOK.md`
Combined log entries in version order:
- **v0.251–v0.254** from `main` (enemy display, canyon verticality, rooms HUD, lock-on panel)
- **v0.255** from the ticket branch (level-2 mechanics reference)
- Final **Remaining gaps: None** at the end

No conflict markers remain in either file. The harness can commit and run the test suite.
