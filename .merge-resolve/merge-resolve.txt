`LOGBOOK.md` is resolved.

The conflict was two independent review entries landing at the same spot after v0.286. The merged file keeps **both**:

1. **v0.289 — 291-burning-status-effect-foundation** (from `main`) — burning status helpers, tick damage, and client flame animation
2. **v0.290 — 302-chain-lightning-card** (from this branch) — Voltaic Chain spell, arc rendering, and debug scenario review

Each entry has its own `## Remaining gaps` section, matching the logbook pattern elsewhere. All conflict markers are removed; the harness can finish the merge.
