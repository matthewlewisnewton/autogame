`LOGBOOK.md` is resolved. The conflict was at the end of the logbook: `main` added the **v0.333 — 365-anim-spike-trap** review entry while the branch added **v0.336 — 370-playthrough-revalidate-sunken-canyon**.

The merged file keeps both entries in version order:

1. **v0.333** (from `main`) — Spike Trap animation/timing review, with its own “Remaining gaps” section  
2. **v0.336** (from the branch) — Sunken Canyon revalidation playthrough review, with its own “Remaining gaps” section  

All conflict markers are removed. The harness can proceed with the merge commit and test run.
