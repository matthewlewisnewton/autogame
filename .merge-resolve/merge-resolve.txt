Merge conflicts are resolved. All three files are marker-free and `game/client/main.js` passes a syntax check.

### `game/client/main.js`
Combined booth wiring from both sides:
- **Imports**: `boothPrompt` (`BOOTH_ACTION_EVENT`), `boothDeck` (deck terminal), and `launchBooth` (mission launch)
- **Listeners**: `registerDeckBoothListener` + `?booth=deck` debug opener **and** a `BOOTH_ACTION_EVENT` handler for the character booth
- **Debug hooks**: deck test hooks (`__openDeckBoothForTest`, `__requestDebugBoothOpenForTest`) **and** `requestBoothDebugOpen()` for `?booth=character`

Launch booth behavior elsewhere in the file is unchanged.

### `TASKS.md`
Both cleanup backlog entries are kept:
- `278-cleanup-booth-mission-launch` (from main)
- `278-cleanup-booth-deck-terminal` (from this ticket)

### `LOGBOOK.md`
All review entries are kept in version order (v0.240 → v0.241 → v0.242 → v0.243), with a single final “Remaining gaps: None” section.

Spot-checked booth-related client tests (17 + deck booth tests) — all passed; exit code 1 was only from partial-run coverage thresholds. The harness can commit and run the full suite.
