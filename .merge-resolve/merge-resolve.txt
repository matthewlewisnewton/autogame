Resolved the `LOGBOOK.md` conflict by combining both sides in chronological version order:

1. **v0.264** — 241-free-hat-swap (from `main`)
2. **v0.265** — 256-level2-sunken-canyon (from `main`)
3. **v0.266** — 243-retire-2d-lobby-menus (from `main`)
4. **v0.267** — 213-net-shared-event-name-constants (from `main`)
5. **v0.268** — 258-miniboss-encounter-framework (from the ticket branch)

All conflict markers are removed. The logbook now records every ticket that landed on `main` since v0.261, with the miniboss encounter framework entry last. The harness can proceed with commit and merge.
