`LOGBOOK.md` is resolved.

Both sides were independent logbook entries that landed after `v0.307`. I kept **both**, in chronological order:

1. **v0.309 — 313-saber-of-light-aoe-per-grind-scaling** (from `main`, 23:09:16) — Saber AOE per-grind scaling review
2. **v0.313 — 311-slower-scaling-early-strong-cards-plus-astral-trim** (from this branch, 23:39:26) — per-card grind scale, Astral Guardian trim, arena-trials debug scenario fix

Each entry retains its own `## Remaining gaps / None.` section, matching the logbook pattern on both sides. All conflict markers are removed; the harness can proceed with the merge.
