Merge conflicts are resolved in both files.

### `game/server/progression.js`
Kept **both** import groups:
- Ticket: `cardStats.json` and `cardEconomy.json` (`CARD_STATS`, `EVOLUTION_TRANSFORMS`, `CARD_SELL_VALUES`)
- Main: `PHASES`, `setGamePhase`, `isLobbyPhase`, `isPlayingPhase` from `./lobbies`

`getObjectiveDef` from `./objectives` was already present above the conflict. Syntax check passes.

### `LOGBOOK.md`
Merged chronologically: **v0.195** (input keymap), **v0.196** (lobby phase machine), **v0.197** (hat unlock ordering), then **v0.198** (card def drift).

All conflict markers are removed. The harness can commit and finish the merge.
