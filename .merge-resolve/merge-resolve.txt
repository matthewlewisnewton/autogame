`LOGBOOK.md` is resolved.

Both sides landed at the same place in the log (after v0.198), so the fix was to keep **all three** version entries in order:

1. **v0.202** — `216-lobby-remove-dead-hostid` (from `main`)
2. **v0.203** — `228-gameplay-card-cinder-snare` (from `main`)
3. **v0.204** — `214-lobby-clear-ready-on-disconnect` (from this ticket branch)

Conflict markers are removed; there are no remaining `<<<<<<<` / `=======` / `>>>>>>>` markers in the repo. The harness can finish the merge and run tests.
