Merge conflicts are resolved in both files by combining both sides rather than picking one.

**`TASKS.md`** — Both cleanup backlog entries are kept, in ticket order:
- `376-cleanup-height-aware-projectile-aiming` (from `main`)
- `377-cleanup-airborne-flying-entity-support` (from this branch)

**`LOGBOOK.md`** — Both review log entries are kept, in version order:
- `v0.338 — 375-height-aware-projectile-aiming` (from `main`)
- `v0.340 — 376-airborne-flying-entity-support` (from this branch)

Each log entry retains its own "Remaining gaps / None." section. All conflict markers are removed. The harness can proceed with the merge commit and test run.
