All four conflicted files are resolved and marker-free. Summary of how each merge was combined:

**`game/shared/dungeonTheme.json`**
- Kept main's warm sandy `open-plaza` palette (`#b8a078` / `#e8a317` accent) from plaza-arena-identity
- Added the ticket's separate `sunken-canyon` profile with band-specific floor tints

**`game/client/dungeon.js`**
- `buildLandmarkMesh` now supports both landmark types: `canyon_monolith` (ticket) and `arena_dais` (main), each with its own switch case

**`game/client/test/dungeon.test.js`**
- Imports both `buildCanyonCliffLipMesh` and `buildCoverMesh`
- Landmark test covers both monolith and dais rendering
- Updated the palette test to expect main's sandy `open-plaza` floor (`0xb8a078`) while keeping sunken-canyon band assertions (`plateau`/`canyon` hex values)

**`LOGBOOK.md`**
- Preserved main's v0.247–v0.251 entries, then appended the ticket's v0.252 canyon-verticality-identity entry

Dungeon tests pass (872/872). The harness can commit and complete the merge.
