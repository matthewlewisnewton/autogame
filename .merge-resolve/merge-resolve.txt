All merge conflicts are resolved. Both sides’ intent is preserved:

**Enemy registries** (`models.js`, `renderer.js`, `config.js`, `simulation.js`) — All three stage-boss types are registered:
- `annex_overseer` (Training Caverns / vault)
- `arena_champion` (Open Plaza)
- `spire_warden` (Spire Ascent)

**Objective copy** (`theme.json`, `questBoard.js`, `quests.js`) — Stage-boss summaries route by quest (`spire_ascent`, `canyon_descent`) and by `bossType` (`annex_overseer`), with trial-warden as the default.

**Progression** — Party-size HP scaling applies to `miniboss`, `annex_overseer`, and `spire_warden`.

**Tests** — Registry, renderer, quest-board, enemy-display, and server `ENEMY_DEFS` tests cover all boss types.

**LOGBOOK.md** — v0.272 (this ticket) is kept above v0.271/v0.270/v0.269 from main.

Focused validation: 62 tests passed across the merged files; `theme.json` is valid. No conflict markers remain in the listed files. The harness can commit and complete the merge.
