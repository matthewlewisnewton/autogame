{
  "name": "autogame",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"pnpm -C server run dev\" \"pnpm -C client run dev\"",
    "install:all": "pnpm install",
    "test": "vitest run --coverage --config vitest.config.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@vitest/coverage-v8": "^3.2.4",
    "concurrently": "^9.2.1",
    "jsdom": "^26.1.0",
    "socket.io-client": "^4.8.3",
    "vitest": "^3.2.4"
  }
}
